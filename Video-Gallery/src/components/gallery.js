import React from "react";
import { useEffect, useState } from "react";

const Gallery = () => {
  return <div>this is the gallery</div>;
};
export default Gallery;

//    <video width="750" height="500" type="video/mp4">
//    <source src={addVideo}/>
//    {/* we need a url to view the video - we can get this from backend when we sent info over ^^ this will be in courosel page */}
//  </video>
